<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.parallaxscroll.louis"
    android:versionCode="5"
    android:versionName="1.5">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MultipleParallaxExpandableListView"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".SingleParallaxExpandableListView"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".SingleParallaxListView"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".MultipleParallaxListView"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".SingleParallaxScrollView"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".MultipleParallaxScrollView"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".SingleParallaxAlphaScrollView"
            android:label="@string/app_name"></activity>

    </application>

</manifest>
